package com.dreamdiver.rotterdam.util

object Strings {
    // Home Screen
    fun home(isEnglish: Boolean) = if (isEnglish) "Home" else "Thuis"
    fun favorites(isEnglish: Boolean) = if (isEnglish) "Favorites" else "Favorieten"
    fun profile(isEnglish: Boolean) = if (isEnglish) "Profile" else "Profiel"
    fun more(isEnglish: Boolean) = if (isEnglish) "More" else "Meer"

    // More Screen
    fun moreOptions(isEnglish: Boolean) = if (isEnglish) "More Options" else "Meer Opties"
    fun languageSettings(isEnglish: Boolean) = if (isEnglish) "Language Settings" else "Taalinstellingen"
    fun changeLanguage(isEnglish: Boolean) = if (isEnglish) "Change Language" else "Taal Wijzigen"
    fun englishDutch(isEnglish: Boolean) = if (isEnglish) "English / Dutch" else "Engels / Nederlands"

    fun information(isEnglish: Boolean) = if (isEnglish) "Information" else "Informatie"
    fun notice(isEnglish: Boolean) = if (isEnglish) "Notice" else "Kennisgeving"
    fun viewImportantNotices(isEnglish: Boolean) = if (isEnglish) "View important notices" else "Belangrijke kennisgevingen bekijken"
    fun aboutUs(isEnglish: Boolean) = if (isEnglish) "About Us" else "Over Ons"
    fun learnAbout(isEnglish: Boolean) = if (isEnglish) "Learn about Rotterdam City" else "Leer meer over Rotterdam City"

    fun legal(isEnglish: Boolean) = if (isEnglish) "Legal" else "Juridisch"
    fun privacyPolicy(isEnglish: Boolean) = if (isEnglish) "Privacy Policy" else "Privacybeleid"
    fun howWeHandleData(isEnglish: Boolean) = if (isEnglish) "How we handle your data" else "Hoe we uw gegevens verwerken"
    fun termsConditions(isEnglish: Boolean) = if (isEnglish) "Terms & Conditions" else "Algemene Voorwaarden"
    fun termsOfService(isEnglish: Boolean) = if (isEnglish) "Terms of service" else "Servicevoorwaarden"

    fun other(isEnglish: Boolean) = if (isEnglish) "Other" else "Overig"
    fun rateUs(isEnglish: Boolean) = if (isEnglish) "Rate Us" else "Beoordeel Ons"
    fun rateOnPlayStore(isEnglish: Boolean) = if (isEnglish) "Rate on Play Store" else "Beoordeel op Play Store"
    fun shareApp(isEnglish: Boolean) = if (isEnglish) "Share App" else "App Delen"
    fun shareWithFriends(isEnglish: Boolean) = if (isEnglish) "Share with friends" else "Deel met vrienden"

    fun version(isEnglish: Boolean) = if (isEnglish) "Version 1.0.0" else "Versie 1.0.0"


    // Menu Items
    fun dailyOffer(isEnglish: Boolean) = if (isEnglish) "Daily Offer" else "Dagelijkse Aanbieding"
    fun jobService(isEnglish: Boolean) = if (isEnglish) "Job Service" else "Banen Service"
    fun diagnostic(isEnglish: Boolean) = if (isEnglish) "Diagnostic" else "Diagnostiek"
    fun hospital(isEnglish: Boolean) = if (isEnglish) "Hospital" else "Ziekenhuis"
    fun doctor(isEnglish: Boolean) = if (isEnglish) "Doctor" else "Dokter"
    fun ambulance(isEnglish: Boolean) = if (isEnglish) "Ambulance" else "Ambulance"
    fun bloodDonor(isEnglish: Boolean) = if (isEnglish) "Blood Donor" else "Bloeddonor"
    fun thanaPolice(isEnglish: Boolean) = if (isEnglish) "Police Station" else "Politiebureau"
    fun lawyer(isEnglish: Boolean) = if (isEnglish) "Lawyer" else "Advocaat"
    fun journalist(isEnglish: Boolean) = if (isEnglish) "Journalist" else "Journalist"
    fun onlineService(isEnglish: Boolean) = if (isEnglish) "Online Service" else "Online Dienst"
    fun fireService(isEnglish: Boolean) = if (isEnglish) "Fire Service" else "Brandweer"
    fun electricityOffice(isEnglish: Boolean) = if (isEnglish) "Electricity Office" else "Elektriciteitskantoor"
    fun todayNews(isEnglish: Boolean) = if (isEnglish) "Today's News" else "Nieuws van Vandaag"
    fun touristSpot(isEnglish: Boolean) = if (isEnglish) "Tourist Spot" else "Toeristische Attractie"
    fun busTime(isEnglish: Boolean) = if (isEnglish) "Bus Time" else "Bus Tijden"
    fun trainTime(isEnglish: Boolean) = if (isEnglish) "Train Time" else "Trein Tijden"
    fun carRental(isEnglish: Boolean) = if (isEnglish) "Car Rental" else "Autoverhuur"
    fun educational(isEnglish: Boolean) = if (isEnglish) "Educational" else "Onderwijs"
    fun teacher(isEnglish: Boolean) = if (isEnglish) "Teacher" else "Leraar"

    // Slider
    fun cumillaCity(isEnglish: Boolean) = if (isEnglish) "Rotterdam City" else "Rotterdam City"
    fun allServicesInOnePlace(isEnglish: Boolean) = if (isEnglish) "All services in one place" else "Alle diensten op één plek"
    fun emergencyServices(isEnglish: Boolean) = if (isEnglish) "Emergency Services" else "Nooddiensten"
    fun hospitalDoctorAmbulance(isEnglish: Boolean) = if (isEnglish) "Hospital, Doctor, Ambulance" else "Ziekenhuis, Dokter, Ambulance"
    fun dailyOffers(isEnglish: Boolean) = if (isEnglish) "Daily Offers" else "Dagelijkse Aanbiedingen"
    fun specialDiscounts(isEnglish: Boolean) = if (isEnglish) "Get special discounts" else "Ontvang speciale kortingen"
    fun transportService(isEnglish: Boolean) = if (isEnglish) "Transport Service" else "Vervoersdienst"
    fun busTrainSchedule(isEnglish: Boolean) = if (isEnglish) "Bus and Train Schedule" else "Bus en Trein Dienstregeling"

    // Common UI Text
    fun back(isEnglish: Boolean) = if (isEnglish) "Back" else "Terug"
    fun close(isEnglish: Boolean) = if (isEnglish) "Close" else "Sluiten"
    fun cancel(isEnglish: Boolean) = if (isEnglish) "Cancel" else "Annuleren"
    fun ok(isEnglish: Boolean) = if (isEnglish) "OK" else "OK"
    fun yes(isEnglish: Boolean) = if (isEnglish) "Yes" else "Ja"
    fun no(isEnglish: Boolean) = if (isEnglish) "No" else "Nee"
    fun save(isEnglish: Boolean) = if (isEnglish) "Save" else "Opslaan"
    fun edit(isEnglish: Boolean) = if (isEnglish) "Edit" else "Bewerken"
    fun delete(isEnglish: Boolean) = if (isEnglish) "Delete" else "Verwijderen"
    fun retry(isEnglish: Boolean) = if (isEnglish) "Retry" else "Opnieuw proberen"
    fun loading(isEnglish: Boolean) = if (isEnglish) "Loading..." else "Laden..."
    fun error(isEnglish: Boolean) = if (isEnglish) "Error" else "Fout"
    fun success(isEnglish: Boolean) = if (isEnglish) "Success" else "Succes"

    // Search
    fun search(isEnglish: Boolean) = if (isEnglish) "Search" else "Zoeken"
    fun searchForAnything(isEnglish: Boolean) = if (isEnglish) "Search for anything" else "Zoek naar alles"

    // Services
    fun services(isEnglish: Boolean) = if (isEnglish) "Services" else "Diensten"
    fun allServices(isEnglish: Boolean) = if (isEnglish) "All Services" else "Alle Diensten"
    fun noServicesAvailable(isEnglish: Boolean) = if (isEnglish) "No services available" else "Geen diensten beschikbaar"
    fun serviceDetails(isEnglish: Boolean) = if (isEnglish) "Service Details" else "Service Details"
    fun frequentlyUsed(isEnglish: Boolean) = if (isEnglish) "— Frequently used —" else "— Vaak gebruikt —"

    // Trending
    fun trending(isEnglish: Boolean) = if (isEnglish) "Trending on" else "Trending"
    fun trendingDetails(isEnglish: Boolean) = if (isEnglish) "Trending Details" else "Trending Details"

    // Descriptions
    fun description(isEnglish: Boolean) = if (isEnglish) "Description" else "Beschrijving"
    fun noDescriptionAvailable(isEnglish: Boolean) = if (isEnglish) "No description available" else "Geen beschrijving beschikbaar"
    fun summary(isEnglish: Boolean) = if (isEnglish) "Summary" else "Samenvatting"
    fun details(isEnglish: Boolean) = if (isEnglish) "Details" else "Details"
    fun fullDetails(isEnglish: Boolean) = if (isEnglish) "Full Details" else "Volledige Details"

    // Contact Information
    fun phoneNumber(isEnglish: Boolean) = if (isEnglish) "Phone Number" else "Telefoonnummer"
    fun phone(isEnglish: Boolean) = if (isEnglish) "Phone" else "Telefoon"
    fun address(isEnglish: Boolean) = if (isEnglish) "Address" else "Adres"
    fun location(isEnglish: Boolean) = if (isEnglish) "Location" else "Locatie"
    fun openInGoogleMaps(isEnglish: Boolean) = if (isEnglish) "Open in Google Maps" else "Open in Google Maps"
    fun map(isEnglish: Boolean) = if (isEnglish) "Map" else "Kaart"

    // Dates
    fun published(isEnglish: Boolean) = if (isEnglish) "Published" else "Gepubliceerd"
    fun created(isEnglish: Boolean) = if (isEnglish) "Created" else "Aangemaakt"
    fun unknown(isEnglish: Boolean) = if (isEnglish) "Unknown" else "Onbekend"

    // Articles
    fun readFullArticle(isEnglish: Boolean) = if (isEnglish) "Read Full Article" else "Lees volledig artikel"
    fun openInBrowser(isEnglish: Boolean) = if (isEnglish) "Open in browser" else "Open in browser"

    // Subcategories
    fun noSubcategoriesAvailable(isEnglish: Boolean) = if (isEnglish) "No subcategories available" else "Geen subcategorieën beschikbaar"

    // Exit Dialog
    fun exitApp(isEnglish: Boolean) = if (isEnglish) "Exit App" else "App afsluiten"
    fun exitConfirmation(isEnglish: Boolean) = if (isEnglish) "Are you sure you want to exit?" else "Weet je zeker dat je wilt afsluiten?"
    fun exit(isEnglish: Boolean) = if (isEnglish) "Exit" else "Afsluiten"

    // Slider
    fun sliderDetails(isEnglish: Boolean) = if (isEnglish) "Slider Details" else "Slider Details"

    // Profile
    fun editProfile(isEnglish: Boolean) = if (isEnglish) "Edit Profile" else "Profiel Bewerken"
}

